# Настройка таблиц для долгов

## Проблема: данные не сохраняются

Если данные не сохраняются в таблицы `customer_debts` и `supplier_debts`, возможно таблицы не созданы в базе данных.

## Решение 1: Запустить миграцию

```bash
cd backend_nestjs
npm run migration:run
```

## Решение 2: Создать таблицы вручную через SQL

Если миграция не работает, выполните SQL скрипт:

```bash
mysql -u root -p all_shops < scripts/create-debts-tables.sql
```

Или через MySQL Workbench/клиент выполните содержимое файла `scripts/create-debts-tables.sql`

## Проверка существования таблиц

Выполните в MySQL:

```sql
USE all_shops;
SHOW TABLES LIKE '%debt%';
```

Должны быть видны:
- `customer_debts`
- `supplier_debts`

## Проверка структуры таблиц

```sql
DESCRIBE customer_debts;
DESCRIBE supplier_debts;
```

## Проверка данных

```sql
SELECT * FROM customer_debts;
SELECT * FROM supplier_debts;
```

## Логирование

После запуска backend сервера, при попытке создать долг, в консоли должны появиться логи:
- `📥 POST /customer-debts:` - запрос получен
- `📝 Создание долга клиента:` - начало создания
- `✅ Долг клиента сохранен:` - успешное сохранение

Если видите ошибки, проверьте:
1. Существуют ли таблицы в базе
2. Правильно ли настроены переменные окружения в `.env`
3. Есть ли права доступа у пользователя БД
